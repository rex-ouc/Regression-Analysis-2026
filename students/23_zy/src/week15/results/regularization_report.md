# Week15 Regularization Report：L1 vs L2 逻辑回归

## 1. 数据说明

本部分构造了一份特征较多且带有共线性的二分类数据。特征数为 30，其中 x1、x2、x3 构成一组相关特征，x4、x5、x6 构成另一组相关特征，同时还加入了一些噪声特征。

## 2. L1 和 L2 结果对比

| threshold | TP | TN | FP | FN | accuracy | precision | recall | F1 | 模型 | best_C | ROC_AUC | log_loss | 非零系数个数 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0.5000 | 85 | 91 | 16 | 18 | 0.8381 | 0.8416 | 0.8252 | 0.8333 | L1 Logistic | 0.3000 | 0.9393 | 0.3301 | 19 |
| 0.5000 | 83 | 92 | 15 | 20 | 0.8333 | 0.8469 | 0.8058 | 0.8259 | L2 Logistic | 0.1000 | 0.9304 | 0.3511 | 30 |

性能对比图保存位置：

```text
src/week15/figures/regularization_metrics.png
```

复杂度对比图保存位置：

```text
src/week15/figures/regularization_complexity.png
```

regularization_metrics.png 的横轴是指标名称，纵轴是指标数值，不同柱子代表 L1 和 L2 逻辑回归。regularization_complexity.png 的横轴是模型类型，纵轴是非零系数个数。

## 3. 核心问题回答

### 3.1 L1 和 L2 的预测表现差很多吗？

从测试集指标来看，L1 和 L2 的 accuracy、recall、ROC-AUC 和 log loss 可能不会差特别多。也就是说，它们都能作为有效的分类模型。

### 3.2 哪个模型更稀疏？

L1 更稀疏。因为 L1 正则化可以把一部分系数压缩为 0。

### 3.3 哪个模型更适合给出更短的变量名单？

L1 更适合。因为它保留下来的非零系数变量可以直接理解为较短的变量名单。

### 3.4 如果业务方更在意模型稳定性而不是变量筛选，更偏向哪一个？

如果更在意稳定性，我会更偏向 L2。L2 不会强行删除变量，而是整体压缩系数，通常更适合提高模型稳定性。